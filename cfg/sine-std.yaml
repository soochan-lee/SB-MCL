include:
  - cfg/data/sine.yaml
  - cfg/model/std.yaml

encoder: Mlp
enc_args:
  layers: 2
  hidden_dim: 512
  output_activation: relu
  output_shape: [512]

mlp_args:
  hidden_dim: 512
  layers: 2
  output_activation: none

decoder: Identity
dec_args: {}
